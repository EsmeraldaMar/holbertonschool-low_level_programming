0)it prints a name.
1) it executes a function that is given as a parameter on each element of an array, action is a pointer to the function we need to use and size is the size of an array.
2) it searches for an integer, size is no of elements in aray, cmp is pointer to function to be used to compare values, int_index returns index of first element, if no element matches return -1 and if size is <= 0, return -1.
3) It performs simple operations, basically a little calculator.

