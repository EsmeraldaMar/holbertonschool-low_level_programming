#include "main.h"
/**
 * add - it adds two integers together
 * @a: the first integer to be added.
 * @b: the second integer to be added to a.
 * description: it adds two integers together and prints out the result
 *
 * Return: result of added integers
 */
int add(int a, int b)
{
	return (a + b);
}
