#include "main.h"
/**
 * mul - it multiplies two integers
 * @a: is first integer we multiply 
 * @b: is second integer we multiply with 'a'
 *
 * description: we multiply two integers together
 *
 * Return: Result of multiplied two integers
 */
int mul(int a, int b)
{
	return ( a * b );
}
